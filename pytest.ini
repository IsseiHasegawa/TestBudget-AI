[pytest]
testpaths = demo_project/tests tests_internal
norecursedirs = .* build dist *.egg venv .venv node_modules src artifacts data
markers =
    slow: test whose runtime is dominated by simulated I/O latency
